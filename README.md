# OpenMind
Upload your ChatGPT export → ML pipeline (BERTopic, DistilRoBERTa, DeBERTa, BERT-NER) discovers topics, emotions, personality → LLM reads actual messages and names everything → 96KB interactive dashboard with 3D character, brain map, wellness gauge, 10 tabs. Runs free on Google Colab.
